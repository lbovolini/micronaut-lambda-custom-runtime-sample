datasources:
  default:
    dialect: H2
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:test
    username: "sa"
    password: ""

jpa:
  default:
    entity-scan:
      packages: com.github.lbovolini.model
    properties:
      hibernate:
        hbm2ddl:
          auto: update
        show_sql: true